<%# 表单项 -%>
<% list.forEach(function(field){ -%>
    <%_ if (field.displayType == 'image') { -%>
    <el-table-column prop="<%= field.key %>" align="center" label="<%= field.label %>">
      <template slot-scope="{ row }">
        <img style="padding: 3px 0; max-width: 100%; max-height: 90px" :src="row.<%= field.key %>" />
      </template>
    </el-table-column>
    <%_ }else if (field.displayType == 'enum') { -%>
    <el-table-column prop="<%= field.key %>" align="center" label="<%= field.label %>">
      <template slot-scope="{ row }">
        <span>{{ <%= field.key %>EnumData(row,'<%= field.key %>') }}</span>
      </template>
    </el-table-column>
    <%_ } else{ -%>
    <el-table-column prop="<%= field.key %>" align="center" label="<%= field.label %>"></el-table-column>
    <%_ } -%>
<%_ }); -%>