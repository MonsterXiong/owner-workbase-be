const COMPONENT_ENUM = {
    COMPONENT:'component',
    ENTRY:'entry'
}


const COMPONENT_CRUD_ENUM={
    QUERY:"query",
    DIALOG:"dialog",
    TABLE:"table",
    ENTRY:COMPONENT_ENUM.ENTRY,
}

module.exports = {
    COMPONENT_ENUM,
    COMPONENT_CRUD_ENUM
}