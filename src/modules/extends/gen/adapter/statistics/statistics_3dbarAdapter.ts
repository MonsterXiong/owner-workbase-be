export function statistics_3dbarAdapter(param){ 
  const { name,pageName, detailParam } = param
  const  { templateParam } = detailParam
  // if(!templateParam || !Object.keys(templateParam)?.length){
  //   return null
  // }
  return param 
}